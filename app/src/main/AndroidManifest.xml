<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.localzes">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".continue_payment"></activity>
        <activity android:name=".Category" />
        <activity android:name=".OrdersSeller" />
        <activity android:name=".AccountsSeller" />
        <activity android:name=".OrdersDetailsUserActivity" />
        <activity android:name=".UpdateShopDetailActivity" />
        <activity android:name=".UpdateProductDetailsActivity" />
        <activity android:name=".NewActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".CurrentOrdersActivity" />
        <activity android:name=".OrdersCompletedActivity" />
        <activity android:name=".OrderOutForDeliveryActivity" />
        <activity android:name=".OrdersAcceptedActivity" />
        <activity android:name=".PastOrdersActivity" />
        <activity android:name=".OrdersDetailsSellerActivity" />
        <activity android:name=".SellerOrdersActivity" />
        <activity android:name=".ManageAddress" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity_New"
            android:label="@string/title_activity_maps__new" />
        <activity android:name=".UserProductsActivity" />
        <activity android:name=".Seller_Products" />
        <activity android:name=".AddProduct" />
        <activity android:name=".SellerShop_detail" />
        <activity
            android:name=".MapsActivity2"
            android:label="@string/title_activity_maps2" />
        <activity android:name=".Home_seller" />
        <activity android:name=".Registerdetails_seller" />
        <activity android:name=".Registerdetails" />
        <activity android:name=".Accounts" />
        <activity android:name=".Search" />
        <activity android:name=".Cart" />
        <activity android:name=".Home" />
        <activity android:name=".Continueas" />
        <activity android:name=".Verify" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".MyFirebaseMessaging"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>

</manifest>